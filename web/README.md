# Flick site

Letterpress landing page. Hero is React Bits `HalftoneReveal` over the brand banner. Motion via registered GSAP effects.

```bash
npm install
npm run dev
```

Production: `npm run build`. GitHub Pages deploys from `web/dist` on push to `main`.
